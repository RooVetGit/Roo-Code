{
	"name": "@roo-code/roo-code-e2e",
	"private": true,
	"scripts": {
		"lint": "eslint --max-warnings=0 src/**/*.ts",
		"check-types": "tsc --noEmit",
		"format": "prettier --write src",
		"test:quick": "pnpm build && npx dotenvx run -f .env.local -- node ./out/runTest.js",
		"test:ci": "pnpm --filter @roo-code/roo-code compile && pnpm --filter @roo-code/roo-code-webview build && pnpm test:quick",
		"build": "rimraf out && tsc -p tsconfig.json"
	},
	"devDependencies": {
		"@types/mocha": "^10.0.10",
		"@types/node": "^22.14.1",
		"@types/vscode": "^1.95.0",
		"@vscode/test-cli": "^0.0.9",
		"@vscode/test-electron": "^2.4.0",
		"glob": "^11.0.1",
		"mocha": "^11.1.0",
		"rimraf": "^6.0.1"
	}
}
